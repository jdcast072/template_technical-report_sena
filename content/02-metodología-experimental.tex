\chapter{Metodología}
Es el método de trabajo. Explica en tiempo pasado cómo se recogieron los datos y los pasos de la práctica para que pueda ser replicada

\input{assets/tables/table-01.tex}

\begin{verbatim}
def saludar():
    print("Hola Mundo")
\end{verbatim}

\begin{lstlisting}[language=SQL]
-- Consulta para obtener los usuarios mayores de edad
SELECT id_usuario, nombre, correo
FROM usuarios
WHERE edad >= 18
ORDER BY fecha_registro DESC;
\end{lstlisting}